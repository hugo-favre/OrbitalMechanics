in order to run the scripts, Python3 is required with the pip packages listed in requirements.txt
These can be installed with the two following commands in bash:
python -m pip install --upgrade pip
python -m pip install -r requirements.txt